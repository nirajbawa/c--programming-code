#include <iostream>
using namespace std;

int main()
{
    // program which check number is positive or negative using if-else 
    // condition

    int a;
    cin >> a; // c in is use to store user input from keyboard

    // positive 
    if(a>0)
    {
        cout << " A is positive "<< endl;
    }
    // negative
    else{
        cout << " A is negative " << endl;
    }
}